id: gate
initial: locked
data:
  granted: { type: bool, external: true }   # unset until the host supplies it
states:
  locked:
    on:
      enter: { target: open, guard: "granted == true" }
  open: {}
