# The smallest useful harel machine: one guarded transition.
# An access gate stays `locked` until the host grants access.
id: gate
initial: locked
data:
  granted: { type: bool, external: true }   # host-supplied; unset until provided
states:
  locked:
    on:
      enter: { target: open, guard: "granted == true" }
  open: {}
